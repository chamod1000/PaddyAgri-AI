# Git Commit Guidelines

- Use short, natural commit messages similar to what a normal student would write.
- Examples: `update project`, `project update`, `code improvements`, `update application`, `project enhancements`, `minor improvements`, `final updates`.
- Do NOT mention implementation details, architecture changes, AI, bug names, internal components, or file names in commit messages.
